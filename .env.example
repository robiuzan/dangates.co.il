# ── דן שערים חשמליים (Dan Electric Gates) environment template ──────────────
# Copy to `.env.local` and fill real values. Never commit `.env.local`.

# Public site URL (used for canonical URLs, sitemap, OG tags)
NEXT_PUBLIC_SITE_URL="https://dangates.co.il"

# Business contact (also mirrored in lib/site-config.ts / site.config.json)
NEXT_PUBLIC_PHONE="072-2587772"
NEXT_PUBLIC_WHATSAPP="972722587772"

# Lead routing — server-side only (public/lead.php; not used on GitHub Pages)
LEAD_EMAIL_TO="info@dangates.co.il"           # 🔶 confirm send-to address
